<section id="services" class="services-split">
  <style>
.services-split {
  width: 100%;
  background: #0D1A5A;       /* navy so edges match the block */
  margin: 0;
  padding: 0;                 /* no outer gutters = no beige strips */
}

.services-inner {
  display: grid;
  grid-template-columns: 1fr; /* mobile: stack */
  max-width: 2000px;
  margin: 0 auto;
  align-items: stretch;
}

.col { width: 100%; }

/* Image first on mobile */
.image-col { background:#0D1A5A; overflow:hidden; }
.image-col img {
  display:block; width:100%; height:100%;
  object-fit:cover; object-position:center;
}

/* Text second on mobile (centered) */
.text-col {
  background:#0D1A5A; color:#f6e4cc; font-family:'Gotham',sans-serif;
  display:flex; align-items:center; justify-content:center;
  text-align:center;
  padding: clamp(1.5rem, 4vw, 2.5rem) clamp(1rem, 3vw, 2rem); /* gutters on text only */
}
.text-wrap { max-width:600px; width:100%; margin:0 auto; }
.text-col h2 { font-size:clamp(1.8rem,3.2vw,2.5rem); margin:0 0 1.5rem; }
.text-col p  { font-size:clamp(1rem,2vw,1.3rem); line-height:1.6; margin:0 0 2rem; }

.cta-btn {
  display:inline-block; background:#ef3741; color:#f6e4cc;
  padding:0.6rem 1.4rem; font-size:clamp(1.1rem,2.6vw,2rem);
  font-weight:bold; text-decoration:none; border-radius:1px;
  transition:all .2s ease; border:2px solid transparent;
  -webkit-tap-highlight-color:transparent;
}
.cta-btn:hover { background:#f6e4cc; color:#0D1A5A; border-color:#0D1A5A; }

/* Desktop: 2 columns with no overflow */
@media (min-width:1100px) {
  .services-inner {
    grid-template-columns: minmax(0,1fr) minmax(0,1fr);
    min-height: 560px;
  }
  .text-col { text-align:left; } /* reset for desktop */
}

  </style>

  <div class="services-inner">
    <!-- MOBILE ORDER: image first -->
    <div class="col image-col">
      <img src="/images/uploads/services-section.jpg" alt="Services visual" loading="lazy" />
    </div>

    <!-- MOBILE ORDER: text second -->
    <div class="col text-col">
      <div class="text-wrap">
        <h2>Strategy, Design & Story that Pay Off</h2>
        <p>
          We help focused teams build brands that lower costs and lift profit.
          Our work aligns your company around a clear brand strategy, then turns it into an identity,
          messaging, and design system that removes guesswork, reduces rework, and delivers a consistent experience customers trust.
          Services include brand strategy, positioning, brand identity design, messaging & brand story,
          and guidelines & activation for web, product, and go-to-market.
        </p>
        <a href="/services" class="cta-btn">See How It Works</a>
      </div>
    </div>
  </div>
</section>
