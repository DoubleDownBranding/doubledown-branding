<section id="services" class="services-split" aria-labelledby="services-title">
  <style>
    :root {
      --navy: #0D1A5A;
      --beige: #f6e4cc;
      --red: #ef3741;
    }

    .services-split {
      width: 100%;
      background: var(--navy);
      margin: 0;
      padding: 0;
    }
    .services-split, .services-split * { box-sizing: border-box; }

    .services-inner {
      display: grid;
      grid-template-columns: 1fr; /* mobile: stack */
      max-width: 2000px;
      margin: 0 auto;
      align-items: stretch;
    }

    .col { width: 100%; }

    /* Image first on mobile */
    .image-col { background: var(--navy); overflow: hidden; }
    .image-col img {
      display: block;
      width: 100%;
      height: auto;              /* fix: no collapse on mobile */
      object-fit: cover;
      object-position: center;
    }

    /* Text second on mobile (centered) */
    .text-col {
      background: var(--navy);
      color: var(--beige);
      font-family: 'Gotham', sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: clamp(1.5rem, 4vw, 2.5rem) clamp(1rem, 3vw, 2rem); /* gutters on text only */
    }
    .text-wrap { max-width: 600px; width: 100%; margin: 0 auto; }
    .text-col, .image-col, .text-wrap { min-width: 0; }
    .text-col h2 { font-size: clamp(1.8rem, 3.2vw, 2.5rem); margin: 0 0 1.5rem; }
    .text-col { -webkit-text-size-adjust: 100%; text-size-adjust: 100%; }
    .text-col h2, .text-col p { overflow-wrap: break-word; hyphens: auto; }
    .text-col p  { font-size: clamp(1rem, 2vw, 1.3rem); line-height: 1.6; margin: 0 0 2rem; }

    .cta-btn {
      display: inline-block;
      background: var(--red);
      color: var(--beige);
      padding: 0.6rem 1.4rem;
      font-size: clamp(1.1rem, 2.6vw, 2rem);
      font-weight: bold;
      text-decoration: none;
      border-radius: 1px;
      border: 2px solid transparent;
      transition: transform .15s ease, background .2s ease, color .2s ease, border-color .2s ease;
      -webkit-tap-highlight-color: transparent;
    }
    .cta-btn:hover { background: var(--beige); color: var(--navy); border-color: var(--navy); }
    .cta-btn:active { transform: translateY(1px); }
    .cta-btn:focus-visible { outline: 3px solid var(--beige); outline-offset: 2px; }

    /* Desktop: 2 columns with equal height */
    @media (min-width: 1100px) {
      .services-inner {
        grid-template-columns: minmax(0,1fr) minmax(0,1fr);
        min-height: 560px;
      }
      .text-col { text-align: left; }
      .image-col img { height: 100%; } /* fill equal-height column */
    }

    @media (prefers-reduced-motion: reduce) {
      .cta-btn { transition: none; }
    }
  </style>

  <div class="services-inner">
    <!-- MOBILE ORDER: image first -->
    <div class="col image-col">
      <img
        src="/images/uploads/services-section.jpg"
        alt="" role="presentation" loading="lazy" decoding="async"
      />
    </div>

    <!-- MOBILE ORDER: text second -->
    <div class="col text-col">
      <div class="text-wrap">
        <h2 id="services-title">Strategy, Design & Story that Pay Off</h2>
        <p>
          We help focused teams build brands that lower costs and lift profit.
          Our work aligns your company around a clear brand strategy, then turns it into an identity,
          messaging, and design system that removes guesswork, reduces rework, and delivers a consistent experience customers trust.
          Services include brand strategy, positioning, brand identity design, messaging & brand story,
          and guidelines & activation for web, product, and go-to-market.
        </p>
        <a href="/services" class="cta-btn">See How It Works</a>
      </div>
    </div>
  </div>
</section>
