<section id="services" class="services-split">
  <style>
    .services-split {
      width: 100%;
      background-color: #f6e4cc;
      margin: 0;
    }

    /* Layout shell */
    .services-inner {
      display: flex;
      flex-direction: column;           /* mobile: stack */
      align-items: stretch;
      max-width: 2000px;
      margin: 0 auto;
    }

    /* Columns */
    .col {
      width: 100%;
    }

    /* Text column */
    .text-col {
      background-color: #0D1A5A;
      color: #f6e4cc;
      font-family: 'Gotham', sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: clamp(1.5rem, 4vw, 2.5rem) clamp(1rem, 3vw, 2rem);
      text-align: left;
    }
    .text-wrap {
      max-width: 600px;
    }
    .text-col h2 {
      font-size: clamp(1.8rem, 3.2vw, 2.5rem);
      margin: 0 0 1.5rem 0;
    }
    .text-col p {
      font-size: clamp(1rem, 2vw, 1.3rem);
      line-height: 1.6;
      margin: 0 0 2rem 0;
    }
    .cta-btn {
      display: inline-block;
      background-color: #ef3741;
      color: #f6e4cc;
      padding: 0.6rem 1.4rem;
      font-size: clamp(1.1rem, 2.6vw, 2rem);
      font-weight: bold;
      text-decoration: none;
      border-radius: 1px;
      transition: all 0.3s ease;
      border: 2px solid transparent;
      -webkit-tap-highlight-color: transparent;
    }
    .cta-btn:hover {
      background-color: #f6e4cc;
      color: #0D1A5A;
      border-color: #0D1A5A;
    }

    /* Image column */
    .image-col {
      overflow: hidden;
      background: #0D1A5A; /* fallback in case image is still loading */
    }
    .image-col img {
      display: block;
      width: 100%;
      height: 100%;
      max-height: none;
      object-fit: cover;
      object-position: center;
    }

    /* Desktop/tablet: side-by-side */
    @media (min-width: 1100px) {
      .services-inner {
        flex-direction: row;            /* split layout */
        min-height: 560px;              /* give it presence without forcing 800px */
      }
      .col {
        width: 50%;
      }
    }

    /* Mobile centering */
    @media (max-width: 1099px) {
      .text-col {
        text-align: center;
        align-items: center;
        justify-content: center;
      }
      .text-wrap {
        margin: 0 auto;
      }
    }
  </style>

  <div class="services-inner">
    <!-- Text first on mobile -->
    <div class="col text-col">
      <div class="text-wrap">
        <h2>Strategy, Design & Story that Pay Off</h2>
        <p>
          We help focused teams build brands that lower costs and lift profit. 
          Our work aligns your company around a clear brand strategy, then turns it into an identity,
          messaging, and design system that removes guesswork, reduces rework, and delivers a consistent experience customers trust.
          Services include brand strategy, positioning, brand identity design, messaging & brand story,
          and guidelines & activation for web, product, and go-to-market.
        </p>
        <a href="/services" class="cta-btn">See How It Works</a>
      </div>
    </div>

    <!-- Image second on mobile -->
    <div class="col image-col">
      <img
        src="/images/uploads/services-section.jpg"
        alt="Services visual"
        loading="lazy"
      />
    </div>
  </div>
</section>
