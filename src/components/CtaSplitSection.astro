<section class="cta-split" aria-labelledby="cta-title">
  <style>
    .cta-split {
      width: 100%;
      background: #f6e4cc;   /* CTA is beige */
      margin: 0;
      padding: 0;            /* no side strip */
    }
    .cta-split, .cta-split * { box-sizing: border-box; }

    .cta-inner {
      display: grid;
      grid-template-columns: 1fr;     /* mobile: stack */
      max-width: 2000px;
      margin: 0 auto;
      align-items: stretch;
    }

    .col { width: 100%; }
    /* allow grid children to shrink so text wraps instead of overflowing */
    .text-col, .image-col, .text-wrap { min-width: 0; }

    /* Image */
    .image-col { background:#f6e4cc; overflow:hidden; }
    .image-col img {
      display:block;
      width:100%;
      height:auto;                    /* fix: natural height on small screens */
      object-fit:contain;
      object-position:center;
      max-height:520px;               /* keep poster reasonable on small screens */
    }

    /* Text */
    .text-col {
      background:#f6e4cc;
      color:#0D1A5A;
      font-family:'Gotham',sans-serif;
      display:flex; align-items:center; justify-content:center;
      text-align:center;
      padding: clamp(1.5rem, 5vw, 4rem) clamp(16px, 4vw, 32px);
      -webkit-text-size-adjust: 100%;  /* stop mobile text inflation */
      text-size-adjust: 100%;
    }
    .text-wrap { max-width:600px; width:100%; margin:0 auto; }
    .text-col h2 { font-size:clamp(1.8rem,3.2vw,2.5rem); margin:0 0 1rem; }
    .text-col, .image-col, .text-wrap { min-width: 0; }
    .text-col p  { font-size:clamp(1rem,2vw,1.3rem); line-height:1.6; margin:0 0 2rem; }
    .text-col h2, .text-col p { overflow-wrap: break-word; hyphens: auto; }
    .text-col { -webkit-text-size-adjust: 100%; text-size-adjust: 100%; }
    .text-col h2, .text-col p { overflow-wrap: break-word; hyphens: auto; }

    .cta-btn {
      display:inline-block; background:#ef3741; color:#f6e4cc;
      padding:0.75rem 2rem; font-size:clamp(1.1rem,2.4vw,2rem);
      font-weight:bold; text-decoration:none; border-radius:1px;
      transition: transform .15s ease, background .2s ease, color .2s ease, border-color .2s ease;
      border:2px solid transparent;
      -webkit-tap-highlight-color:transparent;
    }
    .cta-btn:hover { background:#f6e4cc; color:#0D1A5A; border-color:#0D1A5A; }
    .cta-btn:active { transform: translateY(1px); }
    .cta-btn:focus-visible { outline:3px solid #0D1A5A; outline-offset:2px; }

    /* Desktop: two columns, equal heights */
    @media (min-width:1100px) {
      .cta-inner {
        grid-template-columns: minmax(0,1fr) minmax(0,1fr);
        min-height: 520px;
      }
      .text-col  { text-align:left; }
      .image-col img { max-height:none; height:100%; }  /* fill its half */
    }

    @media (prefers-reduced-motion: reduce) {
      .cta-btn { transition: none; }
    }
  </style>

  <div class="cta-inner">
    <div class="col image-col">
      <img src="/images/uploads/homepagecta.png" alt="Brand strategy session" loading="lazy" decoding="async" />
    </div>

    <div class="col text-col">
      <div class="text-wrap">
        <h2 id="cta-title">Where Big Visions Get Backed.</h2>
        <p>
          We're a brand strategy and identity partner for companies that value focus.
          When your strategy is clear, every decision gets cheaper and your customer experience becomes coherent,
          which means higher conversion, stronger loyalty, and better margins. We turn insight into systems: positioning,
          story, and design that your whole team can use.
        </p>
        <a href="/about" class="cta-btn">Our Why</a>
      </div>
    </div>
  </div>
</section>
